# newrag 财报 RAG 系统 — 项目进展状态报告

> 更新日期：2026-08-10
> 范围：本次会话（含上一阶段收尾）围绕**检索质量治理、嵌入性能、存储可靠性、财报评测集与工程化**所完成的全部工作。
> 项目性质：多模态财报 RAG（解析/切分/百炼向量化/Qdrant 检索/RRF 融合/rerank/DeepSeek 问答）。

---

## 1. 会话目标

本轮聚焦四类问题，逐一落地并验证：

1. **检索质量**：稀疏路噪声治理（文档侧 IDF 重写）与结构块降权/排除是否有收益 → 用召回评测给出量化结论。
2. **入库性能**：批量入库为何慢、是否影响体验 → 定位并发失效 bug 并修复。
3. **存储可靠性**：qdrant 客户端与服务端版本错位、大批量写入超时 → 对齐版本、放宽写超时。
4. **评测体系**：财报 golden 集过于单薄 → 扩充营收/净利/EPS/ROE 多维度；并固化工程约定。

---

## 2. 已完成工作事项

### 2.1 检索质量治理（稀疏路 IDF × 结构块）
- 实现**文档侧 IDF 重写**：入库时对稀疏 index 乘离线统计 IDF，压低高频结构词、抬升判别词。
- 建立**无 IDF 精确对照法**：查询稀疏除以 IDF（点乘还原原始得分，RRF 只用 rank，排序精确还原）。
- 实现**结构块识别与降权**：识别财务表头/页眉/版式说明块，打 `is_structural` 标并置后降权。
- 编写 `eval_idf_structural.py` 做完整方案对比；**结论：结构块多模式零收益，已移除**（见 §2.5）。

### 2.2 嵌入并发优化
- 定位 `ApiEmbedBackend._call` **串行逐批调用**导致 `max_embed_concurrency` 信号量从未生效的 bug。
- 改为 `ThreadPoolExecutor` 并发发送各批，批次顺序由索引数组保证。
- 吞吐实测 **3.1 → 9.9 texts/s（约 3.2 倍）**；并发 4→8→12 为 7.9→9.9→11.0，取 8 为平衡点。

### 2.3 qdrant 版本对齐与写入超时
- 客户端 `qdrant-client` 由 `1.19.0` **降为 `1.13.3`**，与服务端 `qdrant/qdrant:v1.13.4` 对齐（requirements 锁定 `>=1.13,<1.14`）。
- `upsert_chunks` 分批写入补 `timeout=120`（原 30s 默认在负载高时易超时中断、触发重跑/孤儿点）。
- 清理历史上产生的**孤儿点 2777 个**，当前 15 文档 / 25573 点完全一致。

### 2.4 财报数据扩充与评测集
- 财报统一入库至根 `data/uploads`（25 个文件），并启用 **OCR 扫描版**财报实测。
- **扩充 golden 集**：原 8 题 + 新增财报 10 题（茅台/比亚迪/宁德：营收 3 · 净利润 2 · EPS 3 · ROE 2）= **18 题**，全部 rel 非空。
- golden 构造注意：裸数值 snippet 会因子串误匹配（`_norm` 去逗号后 `300,065.66` 含 `65.66`、`11.12%` 命中百分比），EPS/ROE 改用「指标标签|数值」组合提高特异性。

### 2.5 结构块多模式移除
- 评估证明结构块占比仅 **116/25573 ≈ 0.45%**，off/downgrade/exclude 三模式在扩集上完全一致、零收益。
- 据此**移除 `STRUCTURAL_EXCLUDE` 与 off/exclude 分支**，仅保留单一降权行为（`STRUCTURAL_CHUNK_ENABLED=true` 时结构块置后）。

### 2.6 工程化与文档
- 固化 PowerShell 约定到 `backend/scripts/_run_python.ps1`（统一 `Invoke-Py` 调用、UTF-8 BOM 防中文乱码）。
- 更新 `README.md`（并发、IDF 结论、结构块、配置项、常见问题）与 `ablation_report.md`（新增 §5 IDF×结构块存档）。

### 2.7 IDF 按 query 类别动态开关
- 意图路由新增 `query_type`（metric/entity/general）输出，并加**关键词双重分类**兜底（metric 指标词优先，其次 entity 金额词）。
- metric/指标型查询在检索时对查询稀疏**除以 IDF**（撤销文档侧 IDF 加权），entity 类保留 IDF；RRF 只用 rank，排序可精确还原。
- 新增 `idf.neutralize_idf`，在 `answer._search_plan` 中按 `plan.query_type == "metric"` 生效。

### 2.8 字段抽取（结构化指标 → 独立索引）
- 建立指标目录 `app/fields/metrics.py`（营收/净利润/EPS/ROE/毛利率等 + 别名/数值/年份解析）。
- 抽取器 `app/fields/extract.py`：表格模式 A（指标行×年份表头）/模式 B（转置表）+ 正文兜底，去重输出 `FieldRecord`。
- 独立索引 `financial_fields` 表（`app/fields` + registry `replace_fields/query_fields/delete_fields`），入库阶段 2.5 抽取，删除/归档同步清理。
- 问答侧：metric 类问题按 (指标, 年份) 查字段索引，命中则发 `field` SSE 事件并作为强证据前置进知识块；前端展示"字段命中"卡片。

### 2.9 主体公司过滤（消除跨公司噪声）
- **放弃 doc_name 过滤**：复盘发现文件名常不含公司名（`600000_2024年报.pdf`）、英文代码（`AAPL`）、带空格（`五 粮 液`），doc_name 无法覆盖全部场景。
- 改为**内容提取公司名** `extract_company`（扫描前 6 页，`公司简称X` / `XX股份有限公司` 正则），存 `FinancialField.company`。
- 新增 `app/fields/subject.py`：问题主体公司识别（剔除年份/指标别名/填充词 → 双向子串匹配，0/多个匹配返回 None）。
- `query_fields` 增加 `company` 参数；`_lookup_field_evidence` 按 (指标, 年份, 公司) 限定；**无主体时跳过字段注入**（避免多公司混合值当强证据）。
- 修复表格列偏移 `_best_offset`（比亚迪汇总表表头/数据错位 1 列），回填后比亚迪 24 / 宁德 20 / 茅台 26 条。

---

## 3. 技术方案要点

| 方案 | 原理 | 关键点 |
|---|---|---|
| 文档侧 IDF 重写 | 稀疏 index × 文档侧 IDF | 查询端 IDF 反而略降，证明必须文档侧重写 |
| 无 IDF 对照 | 查询稀疏 ÷ IDF | RRF 只用 rank，排序可精确还原 |
| 并发嵌入 | `ThreadPoolExecutor` 并发发批 | 信号量兜底防 429；并发 8 平衡吞吐/稳定 |
| upsert 超时 | 单次写请求 `timeout=120` | 与 `INGEST_TIMEOUT=1800` 双层兜底 |
| golden 构造 | 归一化子串匹配 | EPS/ROE 用「标签\|数值」防误匹配 |

---

## 4. 阶段性成果（量化）

| 指标 | 改动前 | 改动后 | 说明 |
|---|---|---|---|
| 嵌入吞吐 | 3.1 texts/s | **9.9 texts/s** | 约 3.2 倍，并发修复生效 |
| 分块量（浦发年报） | 34,550 块 | **4,135 块** | 约降 15 倍，碎片化收敛 |
| 评估集规模 | 8 题 | **18 题** | 覆盖营收/净利/EPS/ROE 四维度 |
| 结构块占比 | — | 0.45%（116/25573） | 多模式零收益，已移除 |
| qdrant 客户端 | 1.19.0 | **1.13.3** | 与服务端 1.13.4 对齐 |
| 孤儿点 | 2777 | **0** | 15 文档 / 25573 点一致 |
| 18 题字段噪声 | 5 | **1** | 主体过滤消除 4 处跨公司混合值噪声 |
| 18 题整体命中率 | 72.2% | **77.8%** | 字段抽取净收益（主体过滤不改变整体命中，消除误导风险） |

---

## 5. 改动前后对比分析

### 5.1 性能指标
- **嵌入吞吐提升 3.2 倍**：批量入库耗时显著下降，是会话内最重要的性能收益。
- **写入可靠性提升**：`timeout=120` 消除高负载下 30s 写超时中断；孤儿点清零，数据一致性从"偶发重连重跑"变为"干净可复现"。
- **版本错位消除**：客户端/服务端对齐后，避免大版本不一致导致的潜在协议/序列化兼容问题。

### 5.2 功能完整性
- golden 集从"仅营收"扩展到**营收/净利/EPS/ROE 四类指标**，评测覆盖面更全，能暴露 IDF 在标签型问答上的负收益。
- OCR 扫描版财报可用，多模态（PDF 文本 + 扫描图）链路完整。

### 5.3 代码质量
- 移除结构块多模式分支，`_apply_structural` 收敛为单一降权，降低维护面。
- 并发修复用 `ThreadPoolExecutor` + 索引保序，逻辑更清晰、可测（`test_idf_structural.py` 通过）。
- 临时调试脚本（`_dbg_ocr.py`、`_dbg_parse.py`、`_dbg_cninfo.py`、`_dbg_spacex.py`）已清理。

### 5.4 用户体验
- **入库更快、中断更少**：用户不再频繁遇到"超时中断重连重跑"。
- 目录统一到根 `data/uploads`，不再出现散落多个目录的困惑。

---

## 6. 改动后的优势

1. **稀疏路可解释可复现**：IDF 有无的对照方法精确、结论量化，决策有据。
2. **评测体系更可信**：18 题 multidimensional golden，能区分问题类型对方案的敏感度。
3. **工程健壮性提升**：并发生效、写入超时兜底、版本对齐、孤儿点清零，四道防线齐备。
4. **代码更精简**：移除零收益的多模式分支，减少心智负担。

---

## 7. 潜在改进空间

1. **IDF 动态开关已落地**：按 query 类别（metric/entity/general）在检索时动态撤销/保留文档侧 IDF，缓解扩集后 IDF 在标签型指标问答上的负收益。仍建议后续按问题类型拆分统计做量化验证。
2. **字段抽取已落地**：结构化提取指标（净利润/EPS/ROE 等）→ 独立 `financial_fields` 索引，metric 类问答直接精确取值，弥补检索层在指标级表格问答上的短板；并按问题主体公司过滤，消除跨公司混合值噪声（噪声 5→1）。抽取规则仍为启发式，复杂/合并报表口径可继续用 LLM 抽取增强。
3. **在线反馈闭环缺失**：当前无埋点/反馈信号，无法做严苛在线 A/B；需埋点 + 反馈信号 + 负反馈回灌 golden 形成自动回归。
4. **多 worker 依明确**：入库耗时属**离线批吞吐**而非在线 QPS，不构成多 worker 硬理由；多 worker 收益需结合在线查询负载评估。
5. **多用户体系未引入**：当前匿名 session，`org_id/user_visibility` 仅预留扩展点；用户已表达未来多用户倾向，可做独立迭代（JWT/登录页）。

---

## 8. 后续建议（优先级排序）

1. 在线反馈闭环（中）：埋点 + 负反馈回灌 golden，支撑后续 A/B。
2. 多用户体系（低、独立迭代）：JWT/登录页，复用现有 `org_id/user_visibility` 扩展点。
3. 字段抽取增强：对复杂/合并报表口径引入 LLM 抽取，提升覆盖率与口径准确性。
4. IDF 动态开关量化验证：按问题类型拆分统计，确认 metric/entity 分类阈值与收益。