# NewRAG 财报竞品对标 Agent 升级报告

## 1. 升级结果

NewRAG 在保留原有“智能问答、文档库、上传文档”功能的基础上，新增了可独立运行的
“财报竞品对标”模式。它不是简单地让大模型写一段比较结论，而是通过固定、可复现的
六阶段工作流完成：

1. 限定机构、权限、公司、财年和指标范围；
2. 从 `financial_fields` 一次批量读取候选值；
3. 将元、千元、万元、百万元、亿元等数量单位归一为亿元；
4. 通过 Python 确定性计算金额同比或比率百分点变化；
5. 检查缺失值、冲突值和来源知识块；
6. 只基于可比较数据生成排名、趋势结论和可导出的审计报告。

即使没有配置外部 LLM，这条结构化对标链路仍然可以工作。大模型不负责取值、单位换算
或财务计算，因此不会因为生成模型的随机性改变数值结果。

## 2. 用户使用方式

打开前端 `/benchmark`：

1. 输入机构 ID 和数据权限；
2. 选择 2–5 家已入库公司；
3. 选择 1–5 个财年和最多 12 项已抽取指标；
4. 点击“生成可审计对标报告”；
5. 查看数据覆盖、可比覆盖、证据覆盖、冲突数量和六阶段执行状态；
6. 点击任一数值旁的“证据”，核对源文件、页码、章节、知识块和抽取原文；
7. 导出独立 HTML 审计报告，或从“最近分析”重新打开保存结果。

当机构下不足两家公司，或某指标没有结构化字段时，页面不会伪造示例数据，而是提示先
上传并完成财报解析。

## 3. 后端模块

| 文件 | 作用 |
|---|---|
| `backend/app/benchmark/catalog.py` | 定义指标类型、展示单位和“越高越好/越低越好/中性”比较方向 |
| `backend/app/benchmark/schemas.py` | 定义请求、目录、单元格、证据、阶段、摘要和报告响应契约 |
| `backend/app/benchmark/service.py` | 对标编排器：校验范围、批量取数、单位归一、冲突检测、同比计算、结论与置信度 |
| `backend/app/api/v1/benchmark.py` | 提供目录、生成分析、历史列表和历史详情 API |
| `backend/app/models/entities.py` | 新增 `BenchmarkRun`，持久化请求和完整结果快照 |
| `backend/app/store/registry.py` | 新增维度查询、批量字段查询、批量文档读取和对标快照存取 |
| `backend/tests/test_benchmark.py` | 覆盖金额换算、同比、缺失、未知单位、保存读取和错误输入 |

### API

- `GET /api/v1/benchmark/catalog`：返回当前机构实际存在的公司、财年和指标。
- `POST /api/v1/benchmark/analyze`：运行对标工作流，并原子保存结果快照。
- `GET /api/v1/benchmark/runs`：列出最近保存的对标分析。
- `GET /api/v1/benchmark/runs/{id}`：按机构和权限读取某次完整结果。

所有读取继续复用现有 `org_id + visibility` 权限模型。结构化字段采用一次批量 SQL 查询，
不会为每个“公司 × 财年 × 指标”执行一次查询。

## 4. 前端模块

| 文件 | 作用 |
|---|---|
| `frontend/src/views/BenchmarkView.vue` | 对标任务配置、执行状态、覆盖率、公司指标矩阵、证据抽屉、历史和 HTML 导出 |
| `frontend/src/api/benchmark.ts` | 对标 API 客户端 |
| `frontend/src/types.ts` | 与后端响应一致的 TypeScript 类型 |
| `frontend/src/router/index.ts` | 注册 `/benchmark` 懒加载路由 |
| `frontend/src/App.vue` | 在桌面侧边栏和移动端导航加入“竞品对标”入口 |

工作台沿用 NewRAG 的深色财务终端视觉。最显著的界面元素是“六阶段证据轨道”，用于让
用户看到 Agent 完成了哪些确定性步骤，而不是只显示不可解释的加载动画。

## 5. 可信度规则

- `available`：该公司、年份和指标至少存在一个字段候选；
- `comparable`：单位可识别且候选值没有冲突；
- `evidenced`：字段能定位到 `source_chunk_id`；
- 同一单元格存在多个不同候选值时，优先展示表格来源、单位完整、有知识块标识且更新的
  候选，但该单元格不参与排名，并显示冲突警告；
- 金额单位缺失或未知时保留原始值，但不参与单位换算、同比或排名；
- 缺失值保持为空，不由模型推断；
- 金额类指标目前只能统一数量级。现有字段表尚未结构化保存币种、会计准则和合并范围，
  因而含金额的跨公司报告最高标为“需复核”，并明确展示口径警告；
- 只有数据覆盖至少 90%、证据覆盖至少 95%、无冲突且不含上述未结构化金额口径风险时，
  才标记为“高可信”。

## 6. 当前边界与后续升级

当前版本已经是可运行的“结构化对标 Agent MVP”，但不能宣传为自动投资决策系统。

下一阶段应在入库字段中增加：

- 公司统一标识、股票代码和报告类型；
- 原始币种、目标币种和汇率日期；
- 会计准则；
- 合并报表/母公司报表范围；
- 年度、单季度、累计期间；
- 原始披露值、调整后值和重述版本。

完成这些字段后，才能把金额类跨市场比较从“初筛”提升为自动可比。定性竞争分析可继续
复用已有混合检索、多跳子查询和父章节召回，但应该新增任务级评测：结论引用覆盖率、
多方面 `All-Hit@k`、冲突识别召回率和无证据结论率，而不能只看单一 NDCG。

## 7. 验收结果

- 后端：171 项测试通过，其中新增 4 项对标专项测试；
- 前端：16 项 Vitest 测试通过，其中新增 3 项对标 API 测试；
- 浏览器：`/benchmark` 完整生成与证据抽屉回归通过；
- 响应式：375px 页面无水平溢出；
- 无障碍：`/chat`、`/benchmark`、`/documents`、`/upload` 在 375px、768px、1440px 共
  12 组 axe 检查全部通过；
- 工程质量：修改范围的 Ruff、ESLint、Prettier 检查通过；
- 生产构建：Vue TypeScript 检查和 Vite 构建通过，对标页面异步 chunk gzip 约 5.7KB。
