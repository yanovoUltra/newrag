# 开发/离线评估完整环境；保留原安装入口。
# 生产 API / worker 镜像仅使用 requirements-runtime.txt。
-r requirements-runtime.txt

# ===== 官方 RAG 评估 =====
# Ragas 0.4.3 与 langchain-community 0.4.x 存在官方已确认的 VertexAI 导入故障；
# 固定到已完成 import/aevaluate API 冒烟的兼容组合，待上游修复后再升级。
ragas==0.3.9
langchain-community==0.3.31

# ===== 测试 =====
pytest>=8.0
pytest-asyncio>=0.23
