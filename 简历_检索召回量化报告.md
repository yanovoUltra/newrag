# 检索召回优化 · 简历量化报告

> 项目背景：面向财报问答的 RAG 系统，中文年报 + 英文 10-K，多路召回 + 精排 + 结构化字段索引。

***

## 0. 一句话总结

负责企业级 RAG 系统的**检索召回端**：通过六路多源召回、rerank 精排、LLM 路由与问题感知策略，将生产检索质量做到 **NDCG\@8 0.733 / MRR 0.948**，指标题命中率 100%（MRR 1.0），并用数据驱动的方式否掉了 6 项无效方案、建立了一套可复现的消融评测基础设施。

***

## 1. 规模与最终指标

| 维度                      | 数值                                                              |
| ----------------------- | --------------------------------------------------------------- |
| 语料                      | 28,424 块 / 16 份财报（13 家中国公司年报+招股书、工商银行 2025、GE 1999 10-K，中英双语）   |
| 评测集                     | 239 题（168 指标 + 71 非指标；趋势/多跳/综述/比较/对抗 5 类非指标题型）                  |
| **生产检索（真实路径，含 rerank）** | **NDCG\@8 0.7330 / Rec\@8 0.4209 / Rec\@1 0.3002 / MRR 0.9477** |
| 指标题（结构化字段 relay）        | NDCG\@8 0.8318 / **MRR 1.0**（全部相关块排 rank1）科目同义匹配准确率：98.21% (165/168)时间周期准确率：100% (168/168)财务数值提取准确率：92.86% (156/168)                      |
| 非指标题（纯语义检索水平）           | NDCG\@8 0.4994 / MRR 0.8239                                     |
| 满分题（NDCG=1.0）占比         | 44.8%（107/239）                                                  |

> 指标口径说明：指标题有字段索引兜底（命中即 rank1），非指标题才是真实检索水平的反映；两者分开度量，避免虚高。

***

## 2. 核心量化成就（Top 6）

| # | 改动                                                  | 量化收益                                                           | 来源   |
| - | --------------------------------------------------- | -------------------------------------------------------------- | ---- |
| 1 | **六路多源召回增强**（表块语义锚点/公司预过滤/字段 relay/dense 去重/HyDE）   | Recall\@5 **×5.3**、NDCG\@5 **×5.7**（基线 Recall\@5 0.037）        | §1.2 |
| 2 | **rerank 精排引入**（qwen3-rerank，候选 8 + relay/父块 boost） | NDCG\@8 **+305%**（0.1821 → 0.7371，200 题口径）                     | §2   |
| 3 | **非指标题三项召回优化**（章节父块检索/数字单位短语/HyDE）                  | NDCG\@8 **+88.6%**（0.2023 → 0.3815；父块 +62%、HyDE +23%）          | §1.9 |
| 4 | **题型感知策略**（LLM 路由 + 6 类题型差异化处理）                     | 非指标题从 0.25 量级提升到 **0.50**（239 题新测试集口径）                         | 本次   |
| 5 | **评测基础设施**（内容指纹语料重建免疫 + 异步并发评测）                     | 299 题全量评测从十几分钟压缩到 **249.5s**，且语料重建后 golden 无需重标                | §6.4 |
| 6 | **年份语义过滤**（fiscal\_year 精确过滤 + 智能回退 v6-v8）          | 三路召回 Rec\@8 **+58\~82%**；整体 Rec\@8 +9.2%、NDCG\@8 +5.9%、耗时 -11% | §1.6 |

***

## 3. 正收益改动明细（方法 → 量化结果）

### 3.1 多源召回（Recall 端）

- **问题**：基线 Recall\@5 仅 0.037，纯稠密语义检索对"表块/精确数值"极不敏感。
- **方案**：表块语义锚点（嵌入拼文档标题+章节路径）、主体公司预过滤、字段索引回填 relay、dense 内容去重、HyDE 假设文档。
- **结果**：Recall\@5 ×5.3、NDCG\@5 ×5.7（§1.2）；对抗性数值精度题 Top-3 命中 100%（§1.3）。

### 3.2 rerank 精排（排序端，最大单点收益）

- **问题**：三路 RRF 融合后排序质量差（200 题 NDCG 0.18），加权配比调不动。
- **方案**：引入 qwen3-rerank 精排，候选 50→8（越少噪声越少），relay/父块强候选 boost。
- **结果**：NDCG\@8 **0.7371 vs 0.1821（+305%）**，差距 4x+；精排成为质量核心（§2、§1.6 v8）。

### 3.3 结构化字段索引 relay

- **问题**：指标题（"某公司某年净利润是多少"）语义检索对精确数字不可靠。
- **方案**：财务字段独立索引（financial\_fields），公司/指标/年份三重匹配，结果前置注入 top 槽位。
- **结果**：157 指标题 MRR=1.0（全部 rank1）；对**未参与调参的新财报**（工行 2025/GE 1999）同样有效泛化（§5.2）；修复浦发公司名含触发词导致的 17 题 relay 全失后 157/157 题恢复（§6）。

### 3.4 年份语义处理

- **问题**：单年文档问跨年数据时，fiscal\_year 过滤会把相关块滤掉，只剩 1 条 relay。
- **方案**：精确年份过滤 + "无非 relay 真实命中即回退全量"的三级回退 + 趋势/跨年题放宽过滤。
- **结果**：三路召回 Rec\@8 +58\~82%；整体 Rec\@8 +9.2%、NDCG\@8 +5.9%（§1.6 v6-v8）。

### 3.5 路由与题型感知

- **问题**：不同类型题（趋势/多跳/综述/比较/对抗）需要的检索策略不同，一刀切配置互相拖累。
- **方案**：LLM JSON 路由（intent/complexity/query\_type/needs\_hyde）+ 关键词双重判定；metric 关 IDF、analysis 关表格路/relay、综述/趋势/多跳强制 HyDE。
- **结果**：非指标题 NDCG\@8 **+88.6%**（0.2023 → 0.3815；§1.9）；在新测试集（239 题）上非指标题达到 **0.4994 / MRR 0.8239**。

### 3.6 对抗性题专项（8 类问题的工程化修复）

- 数值精度：表块锚点 + 字段回填（Top-3 命中 100%）
- 否定词（"除了 iPhone 之外"）：否定词感知改写，独占块 Top-3 出现 1→0
- 时间混淆：fiscal\_year 精确过滤，跨年块占比 0
- 综述分散：强制 HyDE，Recall\@8 0.20 → 0.37（§1.3）

### 3.7 英文与多语言

- 修复英文大小写 bug（英文题主体 vs 中文公司名不匹配 → relay 全失效）：NDCG +21%；AAPL 10 题、CXMT 27 题恢复正确入库（§1.4）。

***

## 4. 方法论亮点（工程师含金量）

1. **消融驱动的开发流程**：每个改动都进消融网格对比（200+ 题 × 10 配置），用数据说话，不靠直觉。
2. **敢于用数据否掉方案**（避免无效依赖，也是简历亮点）：
   - BM25 词法基线：实测 NDCG 0.014 vs sparse 0.17（低 12 倍）→ **不引入**，删代码；
   - MMR 重排、子查询拆解、rerank 候选放大、加权配比：实测负优化或噪声内 → **不采用**（§1.8）。
3. **评测基建的健壮性**：
   - 内容指纹（sha256）相关集标注 → **语料重建免疫**，chunk\_id 变了 golden 不用重标；
   - 异步并发评测（asyncio + Semaphore + to\_thread）：299 题 **249.5s**，此前十几分钟；
   - 指标/非指标分口径评测，识别并消除 relay 造成的虚高。
4. **泛化验证闭环**：holdout 30 题（全新财报，未参与任何调参）验证"下载→入库→检索"全流程，防止评测集与调参耦合。

***

## 5. 可直接复用的简历条目

**Bullet 版（技术核心）：**

- 构建多路召回 RAG 检索系统（dense/sparse/table 三路 RRF + 结构化字段索引 + 数字短语 + 章节父块六路融合），生产环境 NDCG\@8 0.733 / MRR 0.948，指标类问题命中率 100%（MRR 1.0）。
- 引入 rerank 精排（候选压缩 50→8 + 强候选 boost），相对 RRF 融合检索质量提升 +305%，并通过 200+ 题消融网格验证精排为质量主导项。
- 针对 5 类非指标题型（趋势/多跳/综述/比较/对抗）设计题型感知检索策略（LLM 路由 + HyDE + 年份语义过滤），非指标题检索质量提升 +88.6%。
- 通过内容指纹标注与异步并发评测搭建可复现评测基建：299 题全量评测 249.5s，语料重建后评测集无需重标；以数据驱动拒绝 BM25/MMR 等 6 项无效方案，避免无效技术债。

**量化数字速查（简历可引用）：**

| 指标           | 数值                            |
| ------------ | ----------------------------- |
| 生产检索 NDCG\@8 | 0.733（MRR 0.948）              |
| 指标题命中率       | 100%（MRR 1.0，168 题）           |
| 召回增强         | Recall\@5 ×5.3 / NDCG\@5 ×5.7 |
| rerank 增益    | +305%（vs 无 rerank）            |
| 非指标题优化       | +88.6%（0.2023 → 0.3815）       |
| 语料/评测规模      | 28,424 块 / 239 题 / 中英双语       |
| 评测提速         | 十几分钟 → 249.5s                 |

***

## 6. 技术栈

Python 3.13 / 异步并发（asyncio）/ Qdrant 向量库（稠密+稀疏+表格分路）/ RRF 融合 / qwen3-rerank 精排 / HyDE（deepseek LLM）/ LLM JSON 路由 / 财务字段结构化索引（SQLAlchemy）/ 内容指纹标注（sha256）/ SQLite 评测落库 / RAGAS 式指标体系（NDCG/Recall/MRR/Precision）

***

## 7. 2026-08-12 最新口径校准：候选覆盖跨过 0.9，定位多方面排序瓶颈

> 本节为最新、最严格口径：239 题 = 168 指标 + 71 非指标，非指标题覆盖趋势/多跳/综述/比较/对抗。上文 `NDCG@8 0.733 / MRR 0.948` 是指标与非指标混合的历史整体口径；两者不应直接对比。

### 7.1 最新可验证结果

| 层级 | 指标 | 结果 | 工程含义 |
| --- | --- | ---: | --- |
| 指标类最终排序 | NDCG@1 / Precision@1 / MRR | **1.0000 / 1.0000 / 1.0000** | 结构化字段 relay 稳定把权威答案块排在首位 |
| 指标抽取 | 科目 / 年份 / 数值准确率 | **98.21% / 100% / 98.21%** | 修正百分比 golden 口径误判后的真实结果 |
| 非指标稳定生产路径 | NDCG@8 / Recall@8 / MRR | **0.5045 / 0.4807 / 0.8404** | 当前最终 Top-8 排序口径，不用大 K 虚抬 |
| 非指标深召回 | dense@500 ∪ sparse@500 Recall | **0.9232** | 证明相关证据在大候选池中已能跨过 0.9 |
| 父章节召回 | Recall@50 / @100 | **0.9367 / 0.9968** | 小块找不全时，先找相关章节几乎可完整覆盖 |
| 分层候选池 | Leaf Recall@300 | **0.9087** | “父章节→叶子块”将有效证据覆盖提升到 0.9+ |
| 方面级精排实验 | Parent Recall@50 / All-Hit@50 | **0.9260 / 0.8286** | 按问题方面分路后覆盖显著上升，但延迟与 MRR 不达上线要求 |

### 7.2 最值得写入简历的技术成果

- **建立指标/非指标分层评测体系**：针对 239 道中英文财报问题，引入 NDCG、Recall、MRR、Precision、严格 All-Hit、可达子集 All-Hit 与 Parent All-Hit，避免用单一指标掩盖多证据覆盖不足。
- **构建财报领域多路与分层召回**：dense/sparse/table/field relay/数字短语/章节父块融合，并增加目标公司文档预过滤；非指标大候选池 Recall 达 **0.9232**，父章节 Recall@100 达 **0.9968**。
- **用结构化字段索引解决精确数字检索**：基于公司、指标、年份三重约束回填权威来源块，并确定性优先于概率性 rerank；168 道指标题 **NDCG@1、P@1、MRR 均为 1.0**。
- **通过消融定位“召回→排序”瓶颈转移**：对 Top-K、候选池、BM25 预筛、父章节路由、LLM 方面拆分、qwen3-rerank 与按文档留一监督排序进行实验；确认 0.9+ 覆盖可达，但最终多方面排序需领域监督数据/覆盖感知 reranker。
- **建立“负优化不上线”的性能闭环**：父块 12→50 导致 NDCG@8 0.5045→0.4231，列表式 LLM 选择超过同步延迟预算，线性监督排序在未见公司上 MRR 回归；均及时撤回，避免离线跑分方案损害生产体验。

### 7.3 推荐简历 Bullet（最新口径）

- 负责中英文财报 RAG 检索体系，设计 dense/sparse/table/字段 relay/数字短语/章节父块多路融合与公司级预过滤，将非指标大候选池 Recall 提升至 **0.9232**，父章节 Recall@100 达 **0.9968**。
- 构建财务字段独立索引与权威证据 relay，通过“公司+指标+年份”确定性约束防止语义精排误排，168 道指标题 **NDCG@1 / Precision@1 / MRR 均达 1.0**。
- 搭建 239 题可复现检索评测体系，实现内容指纹标注、指标/非指标分口径、All-Hit/Parent All-Hit 与并发消融，定位系统瓶颈已从召回转移至多方面 rerank，并拒绝多项延迟或排序回归方案上线。

***
